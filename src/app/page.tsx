import TypingSpeedGame from '@/components/typingspeed';

const Home = () => {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-100">
            <TypingSpeedGame />
        </div>
    );
};

export default Home;